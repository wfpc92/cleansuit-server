<!DOCTYPE html>
<html lang="en">
<head>
    <title>Cleansuit | Error <%if (error.status) { %><%= error.status %><% } %></title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta http-equiv="content-type" content="text-html; charset=utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <link rel="stylesheet" href="/javascripts/lib/bootstrap/dist/css/bootstrap.css"/>
    <style>
        body { padding-top:50px; }
    </style>
</head>
<body class="container">

    <main>
        <div class="jumbotron">
            <h2><%if (error.status) { %><%= error.status %> <% } %><%= message %></h2>
            <p>Por favor contacte ventas@cleansuit.co si usted tiene un problema con la Aplicación.</p>
            <%if (error.stack) { %>
            <pre><%= error.stack %> </pre>
            <% } %>
        </div>
    </main>

</body>
</html>
